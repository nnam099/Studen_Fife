# Local development template. Fill empty required values before startup.
NODE_ENV=development
PORT=3000
DB_HOST=127.0.0.1
DB_PORT=55432
DB_USERNAME=student_local
DB_PASSWORD=
DB_NAME=student_finance
# Generate two independent secrets (at least 32 characters each).
JWT_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
